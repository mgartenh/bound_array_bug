mainmodule bound {
    readonly CProxy_Bound boundProxy;
    readonly CProxy_A aProxy;
    readonly CProxy_B bProxy;
    mainchare Bound {
        entry Bound(CkArgMsg *m);
    };

    array [1D]  A { 
        entry A();
        entry void aMethod();
    };

    array [1D]  B { 
        entry B();
    };
};